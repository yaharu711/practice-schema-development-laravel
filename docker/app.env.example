# 注意: 値はここに書かないでください。必要なキーのみ定義します。
# 各自が `cp docker/app.env.example docker/app.env` で複製し、
# `docker/app.env` に実値を設定してください（Git管理外）。

APP_NAME=Laravel
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8080

# CI/ローカル用の既定(Postgresコンテナ)
DB_CONNECTION=pgsql
DB_HOST=db
DB_PORT=5432
DB_DATABASE=app
DB_USERNAME=laravel
DB_PASSWORD=secret

# APP_KEY はここに書かない（`make key` で src/.env に生成）
